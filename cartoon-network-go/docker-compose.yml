
services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: cartoon-backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./backend/uploads:/app/uploads
